// Cursed Baker — entry point
document.addEventListener('DOMContentLoaded', () => {
  ReelGrid.init(document.getElementById('reelGrid'));
  BgParticles.init(document.getElementById('bgParticles'));
  HUD.init();
});
